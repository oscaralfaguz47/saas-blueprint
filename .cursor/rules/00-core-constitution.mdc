---
description: Global constitution for all Relitrue code changes. Non-negotiable architecture, security, tenant isolation, API discipline, and implementation behavior.
alwaysApply: true
---

# Relitrue Core Constitution

This is the global constitution for the Relitrue codebase.

All project instructions live under `.cursor/rules/**`.

When multiple rules apply:
- the more specific rule wins for its domain
- the stricter rule wins when there is tension

## Project Identity

Relitrue is a production-grade multi-tenant SaaS focused on finance-centered request and approval workflows.

Core concerns:
- strict tenant isolation
- strong server-side security
- predictable API contracts
- auditable sensitive actions
- plan and billing enforcement
- resilient and observable architecture
- maintainable and testable codebase

## Official Technology Stack

Use only the approved stack already present in the project:

Frontend and UI:
- Next.js App Router
- React Server Components
- Tailwind CSS
- shadcn/ui

Backend and API:
- Next.js Route Handlers under `src/app/api/**/route.ts`
- NextAuth with JWT strategy

Data layer:
- Prisma ORM
- PostgreSQL

Validation:
- Zod

Language:
- TypeScript

Do not introduce alternative frameworks, routing systems, mutation patterns, ORMs, or validation libraries without explicit approval.

## Non-Negotiable Architecture Rules

- Use Next.js App Router only.
- Do not use `pages/` or `pages/api`.
- Default to Server Components.
- Use Client Components only when strictly necessary.
- Keep Client Components small leaf components.
- Do not use Server Actions.
- All mutations must go through Route Handlers.
- Keep business logic on the server.
- The UI is never the source of truth.
- Reuse existing helpers, schemas, utilities, and patterns before creating new abstractions.
- Modify the minimum number of files necessary.
- Avoid speculative abstractions and unnecessary complexity.

## Multi-Tenancy and Security Rules

- Never trust the client for tenant context, role, permissions, billing state, or plan state.
- Resolve tenant context server-side from the authenticated user and membership records.
- Enforce tenant isolation in every tenant-scoped query and mutation.
- Enforce authentication and authorization server-side.
- Cross-tenant exposure is a critical security bug.
- Do not leak secrets, raw tokens, internal-only details, or unnecessary PII in logs, API responses, or UI output.

## API and Validation Rules

- Validate request body, query parameters, and route parameters with Zod.
- Parse and validate inputs before business logic runs.
- Use predictable and consistent HTTP status codes.
- Use the standard API error shape defined by the API contract rule.
- Do not swallow errors silently.
- Do not partially succeed on sensitive mutations.

## Plans, Billing, and Usage Rules

- Each tenant/workspace must have exactly one subscription.
- Enforce plan limits server-side before executing gated actions.
- Usage counters must be atomic.
- Usage counters must increment only after successful actions.
- Upgrades apply immediately.
- Downgrades do not delete data, but may restrict future actions.

## Auditability and Observability Rules

- Sensitive actions must generate audit logs when applicable.
- Security-relevant failures must be traceable.
- Secrets and raw tokens must never be logged.

## Error Handling and Resilience Rules

- Every Route Handler must have a top-level error boundary.
- Never return raw internal errors, stack traces, or database errors to the client.
- External service calls must always have explicit timeouts.
- Non-critical features must degrade gracefully — critical security paths must fail closed.
- See `error-handling-and-resilience.mdc` for full rules.

## Caching Rules

- Never cache tenant-scoped data without `tenantId` in the cache key.
- Cross-tenant cache contamination is a critical security bug.
- Cached plan and billing state must have a short TTL and must not be used for enforcement decisions on mutations.
- Cache invalidation must be triggered on plan changes, role changes, and user disable events.
- See `caching-strategy.mdc` for full rules.

## Background Job Rules

- Do not perform long-running or unreliable work synchronously inside Route Handlers.
- All background jobs must be idempotent.
- Jobs must enforce tenant isolation and re-validate authorization at execution time.
- Export, email, and reminder operations must go through a reliable persistent job queue.
- See `background-jobs-and-async.mdc` for full rules.

## Environment and Configuration Rules

- All required environment variables must be validated at boot using a centralized Zod schema.
- Server-only secrets must never use the `NEXT_PUBLIC_` prefix.
- Never access `process.env` directly throughout the codebase — use the validated env module.
- Production, staging, and local environments must use fully isolated credentials.
- See `environment-and-configuration.mdc` for full rules.

## API Versioning Rules

- Internal APIs may evolve with coordinated changes.
- External-facing endpoints must use URL versioning (`/api/v1/`).
- Breaking changes to external endpoints require a new version — never change a versioned contract in place.
- Deprecated endpoints must have a documented sunset date and be monitored for active traffic.
- See `api-versioning-and-deprecation.mdc` for full rules.

## Testing and Quality Rules

- Security-critical paths must have explicit negative test cases.
- Every Route Handler must be tested for unauthenticated, unauthorized, and cross-tenant scenarios.
- Bugs must be accompanied by a regression test.
- CI must run the full test suite on every pull request.
- See `testing-and-quality.mdc` for full rules.

## AI Implementation Behavior

For non-trivial changes:
1. Start with a short implementation plan.
2. Identify the minimum files that need to change.
3. Follow existing project patterns.
4. Keep the implementation small, explicit, and production-grade.
5. Do not introduce new architectural patterns without clear justification.
6. Consult the relevant `.cursor/rules/**` file before implementing domain-specific logic.

## Rule Index

| File | Domain |
|---|---|
| `00-core-constitution.mdc` | Global rules — this file |
| `security-multitenancy.mdc` | Tenant isolation and multi-tenancy |
| `authentication-and-session-security.mdc` | Auth, sessions, password reset |
| `authorization-rbac-and-request-access.mdc` | RBAC, permissions, request access |
| `api-contract-validation-errors.mdc` | API error shape, status codes, validation |
| `api-security.mdc` | Rate limiting, idempotency, prototype pollution |
| `application-security.mdc` | XSS, CSRF, SSRF, open redirects |
| `audit-log.mdc` | Audit log requirements and event naming |
| `observability-and-security-operations.mdc` | Logging, alerting, incident response |
| `secrets-and-cryptography.mdc` | Secrets, tokens, webhook replay protection |
| `data-protection-and-privacy.mdc` | PII, data minimization, retention |
| `file-upload-security.mdc` | Upload validation, storage, access |
| `infrastructure-and-edge-security.mdc` | CSP, security headers, HTTPS |
| `plans-usage-billing.mdc` | Subscription model, plan gating, usage counters |
| `prisma-and-performance.mdc` | Queries, transactions, connection pooling, raw SQL safety |
| `caching-strategy.mdc` | Cache keys, TTLs, invalidation, tenant-safe caching |
| `background-jobs-and-async.mdc` | Job queues, exports, emails, retries |
| `error-handling-and-resilience.mdc` | Error boundaries, timeouts, circuit breakers |
| `environment-and-configuration.mdc` | Env var validation, secrets injection, env separation |
| `api-versioning-and-deprecation.mdc` | Versioning strategy, breaking changes, sunset policy |
| `testing-and-quality.mdc` | Test coverage, security test cases, quality gates |
| `dependency-and-supply-chain-security.mdc` | Dependencies, lockfiles, dependency confusion |
| `architecture.mdc` | Domain boundaries, module structure, naming |
| `ui-ux-contract.mdc` | Component structure, screen states, sensitive data |
| `definition-of-done.mdc` | Completion criteria for all changes |

## Done Criteria

A change is not complete unless it:
- compiles successfully
- respects TypeScript types
- preserves tenant isolation
- enforces server-side authentication and authorization where applicable
- includes Zod validation where applicable
- respects the relevant rules under `.cursor/rules/**`
- includes loading, empty, and error states for user-facing screens when applicable
- adds audit logging for sensitive actions when applicable
- includes a top-level error boundary in every Route Handler
- does not introduce security regressions
- has test coverage for new security-relevant behavior
